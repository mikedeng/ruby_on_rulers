require "spec_helper"

RSpec.describe Track11 do
  it "has a version number" do
    expect(Track11::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
